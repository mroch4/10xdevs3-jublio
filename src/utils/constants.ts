// UI-related constants
export const MAX_LABEL_LENGTH = 50;
export const MAX_EVENT_TITLE_LENGTH = 50;

// Firestore collection name constants
export const COLLECTIONS = {
  MILESTONES: "milestones",
  BOOKMARKS: "bookmarks",
} as const;
