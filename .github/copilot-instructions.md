# Copilot Instructions

## Project Guidelines
- Create hooks/ directory for auth related React hooks (and likely other custom hooks in the future). Structure: src/hooks/ for custom React hooks, alongside src/contexts/ and src/components/.